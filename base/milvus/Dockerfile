FROM milvusdb/milvus:v2.5.26

# Milvus 向量数据库镜像（多模态 RAG Demo 用）
# 版本 v2.5.26（2.5 分支最新稳定版，2026年初发布）
# 架构与 2.4 一致，支持 embed 模式（内嵌 etcd + LocalStore，单容器无 minio/etcd/pulsar）
# 含 hybrid search 修复 + BM25 增强等改进
# 用于阿里云镜像构建，供本地 docker pull（Docker Hub 直连不可达时的替代源）
